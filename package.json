{
    "name": "lra",
    "dependencies": {
        "file-saver": "^2.0.5",
        "ol": "^9.2.4",
        "parcel": "^2.12.0",
        "qrcode": "^1.5.3"
    },
    "devDependencies": {
        "buffer": "^6.0.3",
        "deasync": "^0.1.30",
        "parcel": "^2.12.0"
    },
    "scripts": {
        "clean-dist": "rm -rf dist && mkdir -p dist/static/data",
        "copy-assets": "cp -r static/* dist/static",
        "init": "npm run clean-dist && npm run copy-assets",
        "start": "npm run init && touch index.html && parcel index.html --no-cache",
        "build": "npm run init && parcel build --public-url '.' index.html"
    }
}
